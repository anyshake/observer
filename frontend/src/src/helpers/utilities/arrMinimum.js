/**
 * @date 2023-04-11
 * @author Seunghun Lee - 李承訓
 * @name arrMinimum - 数组最小值
 * @description 寻找数组中的最小值
 * @param {Array} arr - 待寻找数组
 * @returns {Number} 最小值
 */
const arrMinimum = (arr) => {
    return Math.min(...arr);
};

export default arrMinimum;
