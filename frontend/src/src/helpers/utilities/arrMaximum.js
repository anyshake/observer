/**
 * @date 2023-04-11
 * @author Seunghun Lee - 李承訓
 * @name arrMaximum - 数组最大值
 * @description 寻找数组中的最大值
 * @param {Array} arr - 待寻找数组
 * @returns {Number} 最大值
 */
const arrMaximum = (arr) => {
    return Math.max(...arr);
};

export default arrMaximum;
