query getStationConfig {
    getStationConfigConstraint {
        namespace
        name
        key
        description
        configType
        isRequired
        currentValue
        options
    }
}
