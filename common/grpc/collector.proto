syntax = "proto3";

option go_package = "/";

message RequestMessage {
	bytes Data = 1;
}

message ResponseMessage {
	bytes Data = 1;
}

service Collector {
	rpc ToDatabase(RequestMessage) returns (ResponseMessage) {}
}